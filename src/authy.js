////////////////////////////////////////////////
// imports
const fs = require('fs');

////////////////////////////////////////////////
// authy
// initialization of our authentication module
const authorize = async (user, pass) => {
	if (!user || !pass) {
		console.log('Authorization requires user id and password.');
	}
};

const configure = async (user, pass) => {
	if (!user || !pass) {
		console.log('Authorization requires user id and password.');
	}
};

configure();
authorize();
